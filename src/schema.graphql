type Books {
    id: ID
    name: String
    description: String
    author: String
    genre: String
}

type DeletedSourceResponse {
    deletedId: ID
    success: Boolean
}

type Query {
    getAllBooks: [Books]
}

